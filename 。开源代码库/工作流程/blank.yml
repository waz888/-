n nname: CI n n#控制工作流何时运行non: n #在推或拉请求事件时触发工作流，但仅适用于“主”分支n push: n分支:[ "main " ] n pull_request: n分支:[" main "]n n n n #允许您从“操作”选项卡手动运行此工作流n workflow_dispatch: n n# 工作流运行由一个或多个可以顺序或并行运行的作业组成njobs: n #此工作流包含一个名为“build”的作业n build: n #作业将在n次运行中运行的运行程序类型-on:Ubuntu-latest n n n # Steps表示将作为作业的一部分执行的一系列任务n steps: n #签出$GITHUB_WORKSPACE下的存储库， 以便您的作业可以访问它n - uses: actions/checkout@v3 n n #使用runners shell运行单个命令n - name:运行一行脚本n run: echo Hello，world！ n n #使用runners shell运行一组命令n - name:运行多行脚本n run: n echo添加其他操作进行构建，n echo测试，并部署您的项目。
